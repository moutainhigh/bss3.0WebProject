<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asia.mapper.billingmapper.ABalanceReturnLogMapper">
  <select id="queryTotalSubsidies"  resultType="java.lang.String">
    select sum(a.return_balance) totalSubsidies
    from a_balance_return_log a
    where a.instance_id=#{instance_id}
  </select>
  <select id="queryRealPerformMonth"  resultType="java.lang.String">
    select count(1) realPerformMonth
    from a_balance_return_log a
    where a.instance_id=#{instance_id}
  </select>
</mapper>